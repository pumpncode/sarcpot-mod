
SMODS.Joker {

	discovered = true,
	key = 'pancake',

	loc_txt = {
		name = 'Pancake',
		text = {
			
		}
	},

	config = { extra = {} },
	loc_vars = function(self, info_queue, card)
		return { vars = {} }
	end,
	rarity = 2,
	atlas = 'sarcpot_atlas',
	pos = { x = 0, y = 0 },
	cost = 7,
	eternal_compat = false,
	calculate = function(self, card, context)		
        
	end
}